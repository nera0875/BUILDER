# MCP SSH Server Configuration

# Server
PORT=3000
HOST=0.0.0.0

# Authentication
BEARER_TOKEN=your-secure-token-here

# SSH Target (default - can be overridden per request)
DEFAULT_SSH_HOST=89.116.27.88
DEFAULT_SSH_PORT=22
DEFAULT_SSH_USER=pilote

# SSH Key Path (inside container)
SSH_PRIVATE_KEY_PATH=/root/.ssh/id_rsa

# Command Security
ALLOWED_COMMANDS=ls,cat,grep,find,ps,df,netstat,systemctl status,pm2 list
BLOCKED_COMMANDS=rm,shutdown,reboot,mkfs,dd,> /dev

# Logging
LOG_LEVEL=info
